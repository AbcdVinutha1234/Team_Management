
{
  "entities": {
    "User": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "User",
      "type": "object",
      "description": "Represents a user within the WorkLink application. Assumes authentication details (like passwords) are managed by an external system.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the User entity."
        },
        "email": {
          "type": "string",
          "description": "The user's email address, used for login and notifications. Must be unique across all users.",
          "format": "email"
        },
        "firstName": {
          "type": "string",
          "description": "The first name of the user."
        },
        "lastName": {
          "type": "string",
          "description": "The last name of the user."
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp indicating when the user account was created.",
          "format": "date-time"
        },
        "updatedAt": {
          "type": "string",
          "description": "Timestamp indicating when the user account was last updated.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "email",
        "firstName",
        "lastName",
        "createdAt"
      ]
    },
    "Project": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Project",
      "type": "object",
      "description": "Represents a project, a container for tasks and associated team members.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Project entity."
        },
        "name": {
          "type": "string",
          "description": "The name of the project."
        },
        "description": {
          "type": "string",
          "description": "A detailed description of the project's scope and goals."
        },
        "status": {
          "type": "string",
          "description": "The current operational status of the project (e.g., 'Active', 'Completed', 'On Hold', 'Archived')."
        },
        "startDate": {
          "type": "string",
          "description": "The planned start date for the project.",
          "format": "date"
        },
        "dueDate": {
          "type": "string",
          "description": "The planned due date for the project.",
          "format": "date"
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp indicating when the project was created.",
          "format": "date-time"
        },
        "updatedAt": {
          "type": "string",
          "description": "Timestamp indicating when the project was last updated.",
          "format": "date-time"
        },
        "createdBy": {
          "type": "string",
          "description": "Reference to the User who created this project. (Relationship: User 1:N Project)"
        }
      },
      "required": [
        "id",
        "name",
        "description",
        "status",
        "createdAt",
        "createdBy"
      ]
    },
    "Task": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Task",
      "type": "object",
      "description": "Represents a specific task within a project, assigned to a team member.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Task entity."
        },
        "title": {
          "type": "string",
          "description": "The title or brief summary of the task."
        },
        "description": {
          "type": "string",
          "description": "A detailed description of the task, potentially augmented by AI suggestions."
        },
        "status": {
          "type": "string",
          "description": "The current status of the task (e.g., 'To Do', 'In Progress', 'Done', 'Blocked', 'Review')."
        },
        "priority": {
          "type": "string",
          "description": "The priority level of the task (e.g., 'Low', 'Medium', 'High', 'Critical')."
        },
        "dueDate": {
          "type": "string",
          "description": "The planned due date for completing the task.",
          "format": "date"
        },
        "projectId": {
          "type": "string",
          "description": "Reference to the Project this task belongs to. (Relationship: Project 1:N Task)"
        },
        "assignedToId": {
          "type": "string",
          "description": "Reference to the User to whom this task is assigned. (Relationship: User 1:N Task)"
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp indicating when the task was created.",
          "format": "date-time"
        },
        "updatedAt": {
          "type": "string",
          "description": "Timestamp indicating when the task was last updated.",
          "format": "date-time"
        },
        "createdBy": {
          "type": "string",
          "description": "Reference to the User who created this task. (Relationship: User 1:N Task)"
        }
      },
      "required": [
        "id",
        "title",
        "status",
        "projectId",
        "assignedToId",
        "createdAt",
        "createdBy"
      ]
    },
    "ProjectMembership": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "ProjectMembership",
      "type": "object",
      "description": "Links a user to a specific project and defines their role within that project, enabling role-based access control per project.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the ProjectMembership entity."
        },
        "projectId": {
          "type": "string",
          "description": "Reference to the Project the user is a member of. (Relationship: Project N:N User via ProjectMembership)"
        },
        "userId": {
          "type": "string",
          "description": "Reference to the User who is a member of the project. (Relationship: User N:N Project via ProjectMembership)"
        },
        "role": {
          "type": "string",
          "description": "The role of the user within this specific project (e.g., 'Admin', 'Member')."
        },
        "joinedAt": {
          "type": "string",
          "description": "Timestamp indicating when the user joined this project.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "projectId",
        "userId",
        "role",
        "joinedAt"
      ]
    }
  },
  "auth": {
    "providers": [
      "password",
      "anonymous"
    ]
  },
  "firestore": {
    "structure": [
      {
        "path": "/users/{userId}",
        "definition": {
          "entityName": "User",
          "schema": {
            "$ref": "#/backend/entities/User"
          },
          "description": "Stores individual user profiles. Access restricted to the user whose ID matches the document ID.",
          "params": [
            {
              "name": "userId",
              "description": "The unique identifier of the user."
            }
          ]
        }
      },
      {
        "path": "/projects/{projectId}",
        "definition": {
          "entityName": "Project",
          "schema": {
            "$ref": "#/backend/entities/Project"
          },
          "description": "Stores project details including a members map for access control.",
          "params": [
            {
              "name": "projectId",
              "description": "Unique identifier for the project."
            }
          ]
        }
      },
      {
        "path": "/tasks/{taskId}",
        "definition": {
          "entityName": "Task",
          "schema": {
            "$ref": "#/backend/entities/Task"
          },
          "description": "Stores individual tasks, denormalized with projectMembers for secure querying.",
          "params": [
            {
              "name": "taskId",
              "description": "Unique identifier for the task."
            }
          ]
        }
      }
    ],
    "reasoning": "Standardized security architecture optimized for real-time collaboration and secure querying. Rules deployment triggered."
  }
}
